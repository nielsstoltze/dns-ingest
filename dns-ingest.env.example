# dns-ingest runtime config.  Copy to dns-ingest.env before installing.
DNS_INGEST_DATA=/home/local_admin/dns-ingest/data
# Optional: push every record to Loki as well.  Leave unset to disable.
# LOKI_PUSH_URL=http://chost11.internal.hoej.eu:3100
